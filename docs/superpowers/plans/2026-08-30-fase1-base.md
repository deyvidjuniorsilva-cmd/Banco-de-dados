# Fase 1 — Base do Projeto (Next.js + Supabase + Auth + Schema) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Colocar de pé o esqueleto do site — projeto Next.js publicável, conectado a um projeto Supabase real, com login funcional e o schema de banco de dados que todas as fases seguintes vão usar.

**Architecture:** Next.js (App Router, TypeScript) hospedado na Vercel. Supabase fornece Postgres + Auth + Storage. Um único usuário (conta única, criada manualmente no Supabase). Rotas protegidas por middleware que checa sessão Supabase; sem sessão, redireciona para `/login`.

**Tech Stack:** Next.js 14+ (App Router), TypeScript, Tailwind CSS, `@supabase/ssr` + `@supabase/supabase-js`, Supabase (Postgres/Auth/Storage), Vitest para testes unitários.

**Spec:** `docs/superpowers/specs/2026-08-30-controle-gastos-design.md`

## Global Constraints

- Uso único-usuário: não construir fluxo de cadastro público — a conta é criada manualmente pelo dono no painel do Supabase.
- Autenticação: email + senha via Supabase Auth (conforme spec).
- Banco de dados: Postgres via Supabase (conforme spec, seção "Modelo de dados").
- Deploy alvo: Vercel (conforme spec).

---

## Antes de começar: pré-requisitos manuais (fora do código)

Estes passos exigem uma conta externa e não podem ser automatizados pelo agente. O humano deve completá-los antes da Task 2:

1. Criar conta em https://supabase.com e criar um novo projeto (região mais próxima do Brasil).
2. Em **Project Settings → API**, copiar `Project URL` e `anon public key`.
3. Em **Authentication → Providers**, confirmar que "Email" está habilitado e que "Confirm email" pode ficar desligado (uso pessoal, sem necessidade de confirmação por email) — ou deixar ligado, à escolha do usuário.
4. Em **Authentication → Users**, criar manualmente o único usuário (email + senha) que vai logar no site.

## File Structure

```
/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                # redireciona para /dashboard ou /login
│   ├── login/
│   │   └── page.tsx            # formulário de login
│   └── dashboard/
│       └── page.tsx            # placeholder protegido, prova que auth funciona
├── lib/
│   └── supabase/
│       ├── client.ts           # cliente Supabase para uso no browser
│       ├── server.ts           # cliente Supabase para uso em server components/actions
│       └── middleware.ts       # helper de refresh de sessão usado pelo middleware
├── middleware.ts                # protege rotas, redireciona sem sessão
├── supabase/
│   └── migrations/
│       └── 0001_init.sql       # schema: accounts, imports, transactions, categories, category_rules, budgets
├── .env.local.example
└── vitest.config.ts
```

---

### Task 1: Scaffold do projeto Next.js

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `app/layout.tsx`, `app/page.tsx`, `.gitignore`, `postcss.config.mjs`, `tailwind.config.ts`, `app/globals.css`

**Interfaces:**
- Produces: projeto Next.js buildável em `npm run build`, servindo `app/page.tsx` em `/`.

- [ ] **Step 1: Rodar o scaffold oficial**

```bash
npx create-next-app@latest . --typescript --tailwind --app --eslint --src-dir=false --import-alias "@/*" --no-turbopack
```

Responda "Yes" se perguntar para instalar em diretório não vazio (a spec já está em `docs/`).

- [ ] **Step 2: Substituir o conteúdo padrão de `app/page.tsx`**

```tsx
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p>Controle de Gastos</p>
    </main>
  );
}
```

- [ ] **Step 3: Verificar que o build funciona**

Run: `npm run build`
Expected: build conclui sem erros, saída lista a rota `/`.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: scaffold Next.js project"
```

---

### Task 2: Cliente Supabase (browser + server)

**Files:**
- Create: `lib/supabase/client.ts`
- Create: `lib/supabase/server.ts`
- Create: `.env.local.example`
- Modify: `.gitignore` (garantir que `.env.local` está ignorado — já vem por padrão no scaffold do Next.js)

**Interfaces:**
- Consumes: variáveis de ambiente `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
- Produces: `createClient()` em `lib/supabase/client.ts` (uso em Client Components) e `createClient()` em `lib/supabase/server.ts` (uso em Server Components/Actions), ambos retornando uma instância `SupabaseClient` de `@supabase/supabase-js`.

- [ ] **Step 1: Instalar dependências**

```bash
npm install @supabase/supabase-js @supabase/ssr
```

- [ ] **Step 2: Criar `.env.local.example`**

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxxxx
```

- [ ] **Step 3: Criar `lib/supabase/client.ts`**

```ts
import { createBrowserClient } from "@supabase/ssr";

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
}
```

- [ ] **Step 4: Criar `lib/supabase/server.ts`**

```ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";

export async function createClient() {
  const cookieStore = await cookies();

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll();
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            );
          } catch {
            // chamado de um Server Component sem permissão de escrita;
            // o middleware cuida do refresh de sessão nesse caso.
          }
        },
      },
    }
  );
}
```

- [ ] **Step 5: Criar `.env.local` real (não versionado) com as credenciais do pré-requisito manual**

Copie `.env.local.example` para `.env.local` e preencha com a `Project URL` e `anon public key` copiadas do Supabase (pré-requisito manual acima).

- [ ] **Step 6: Verificar que o build ainda funciona**

Run: `npm run build`
Expected: build conclui sem erros (as env vars não são exigidas em build time, só em runtime).

- [ ] **Step 7: Commit**

```bash
git add lib/supabase/client.ts lib/supabase/server.ts .env.local.example package.json package-lock.json
git commit -m "feat: add Supabase client helpers"
```

---

### Task 3: Schema do banco de dados

**Files:**
- Create: `supabase/migrations/0001_init.sql`

**Interfaces:**
- Produces: tabelas `accounts`, `imports`, `categories`, `category_rules`, `transactions`, `budgets` no Postgres do Supabase, todas com Row Level Security restringindo acesso ao dono autenticado.

- [ ] **Step 1: Escrever a migração SQL**

```sql
-- 0001_init.sql

create table accounts (
  id uuid primary key default gen_random_uuid(),
  owner uuid not null default auth.uid() references auth.users(id),
  name text not null,
  kind text not null check (kind in ('conta', 'cartao')),
  bank text not null check (bank in ('nubank', 'sicoob_credivar', 'banco_do_brasil', 'mercado_pago')),
  created_at timestamptz not null default now()
);

create table imports (
  id uuid primary key default gen_random_uuid(),
  owner uuid not null default auth.uid() references auth.users(id),
  account_id uuid not null references accounts(id) on delete cascade,
  file_path text not null,
  status text not null default 'pendente' check (status in ('pendente', 'revisado', 'erro')),
  created_at timestamptz not null default now()
);

create table categories (
  id uuid primary key default gen_random_uuid(),
  owner uuid not null default auth.uid() references auth.users(id),
  name text not null,
  created_at timestamptz not null default now(),
  unique (owner, name)
);

create table category_rules (
  id uuid primary key default gen_random_uuid(),
  owner uuid not null default auth.uid() references auth.users(id),
  keyword text not null,
  category_id uuid not null references categories(id) on delete cascade,
  position integer not null default 0,
  created_at timestamptz not null default now()
);

create table transactions (
  id uuid primary key default gen_random_uuid(),
  owner uuid not null default auth.uid() references auth.users(id),
  account_id uuid not null references accounts(id) on delete cascade,
  import_id uuid references imports(id) on delete set null,
  occurred_on date not null,
  description text not null,
  amount numeric(12, 2) not null,
  direction text not null check (direction in ('entrada', 'saida')),
  category_id uuid references categories(id) on delete set null,
  created_at timestamptz not null default now()
);

create table budgets (
  id uuid primary key default gen_random_uuid(),
  owner uuid not null default auth.uid() references auth.users(id),
  category_id uuid not null references categories(id) on delete cascade,
  year integer not null,
  month integer not null check (month between 1 and 12),
  limit_amount numeric(12, 2) not null,
  created_at timestamptz not null default now(),
  unique (owner, category_id, year, month)
);

alter table accounts enable row level security;
alter table imports enable row level security;
alter table categories enable row level security;
alter table category_rules enable row level security;
alter table transactions enable row level security;
alter table budgets enable row level security;

create policy "owner_all_accounts" on accounts for all using (owner = auth.uid()) with check (owner = auth.uid());
create policy "owner_all_imports" on imports for all using (owner = auth.uid()) with check (owner = auth.uid());
create policy "owner_all_categories" on categories for all using (owner = auth.uid()) with check (owner = auth.uid());
create policy "owner_all_category_rules" on category_rules for all using (owner = auth.uid()) with check (owner = auth.uid());
create policy "owner_all_transactions" on transactions for all using (owner = auth.uid()) with check (owner = auth.uid());
create policy "owner_all_budgets" on budgets for all using (owner = auth.uid()) with check (owner = auth.uid());
```

- [ ] **Step 2: Aplicar a migração no Supabase**

Abra o painel do Supabase → **SQL Editor** → cole o conteúdo de `0001_init.sql` → **Run**.
Expected: mensagem de sucesso, 6 tabelas visíveis em **Table Editor**.

- [ ] **Step 3: Verificar RLS manualmente**

No **Table Editor**, confirme que cada uma das 6 tabelas mostra o cadeado "RLS enabled".

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/0001_init.sql
git commit -m "feat: add initial database schema with RLS"
```

---

### Task 4: Middleware de proteção de rotas

**Files:**
- Create: `lib/supabase/middleware.ts`
- Create: `middleware.ts`
- Test: `lib/supabase/middleware.test.ts`

**Interfaces:**
- Consumes: `createServerClient` de `@supabase/ssr`.
- Produces: `updateSession(request: NextRequest): Promise<NextResponse>` exportada de `lib/supabase/middleware.ts`, usada pelo `middleware.ts` raiz.

- [ ] **Step 1: Escrever o teste da função pura de decisão de redirecionamento**

Extraímos a decisão de redirecionar (rota protegida sem usuário → deve redirecionar) para uma função pura testável, separada da parte que fala com a rede (Supabase/cookies).

```ts
// lib/supabase/middleware.test.ts
import { describe, expect, it } from "vitest";
import { shouldRedirectToLogin } from "./middleware";

describe("shouldRedirectToLogin", () => {
  it("redireciona quando não há usuário e a rota não é pública", () => {
    expect(shouldRedirectToLogin({ hasUser: false, pathname: "/dashboard" })).toBe(true);
  });

  it("não redireciona quando há usuário", () => {
    expect(shouldRedirectToLogin({ hasUser: true, pathname: "/dashboard" })).toBe(false);
  });

  it("não redireciona para a própria página de login", () => {
    expect(shouldRedirectToLogin({ hasUser: false, pathname: "/login" })).toBe(false);
  });
});
```

- [ ] **Step 2: Instalar Vitest e rodar o teste para ver falhar**

```bash
npm install -D vitest
```

Crie `vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: { environment: "node" },
});
```

Adicione em `package.json` → `scripts`: `"test": "vitest run"`.

Run: `npm run test -- lib/supabase/middleware.test.ts`
Expected: FAIL — `shouldRedirectToLogin` não existe ainda.

- [ ] **Step 3: Implementar `lib/supabase/middleware.ts`**

```ts
import { createServerClient } from "@supabase/ssr";
import { NextResponse, type NextRequest } from "next/server";

const PUBLIC_PATHS = ["/login"];

export function shouldRedirectToLogin({
  hasUser,
  pathname,
}: {
  hasUser: boolean;
  pathname: string;
}): boolean {
  if (hasUser) return false;
  return !PUBLIC_PATHS.includes(pathname);
}

export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({ request });

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll();
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          );
          response = NextResponse.next({ request });
          cookiesToSet.forEach(({ name, value, options }) =>
            response.cookies.set(name, value, options)
          );
        },
      },
    }
  );

  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (
    shouldRedirectToLogin({
      hasUser: !!user,
      pathname: request.nextUrl.pathname,
    })
  ) {
    const url = request.nextUrl.clone();
    url.pathname = "/login";
    return NextResponse.redirect(url);
  }

  return response;
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

Run: `npm run test -- lib/supabase/middleware.test.ts`
Expected: PASS (3 testes).

- [ ] **Step 5: Criar `middleware.ts` na raiz**

```ts
import { type NextRequest } from "next/server";
import { updateSession } from "@/lib/supabase/middleware";

export async function middleware(request: NextRequest) {
  return updateSession(request);
}

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"],
};
```

- [ ] **Step 6: Commit**

```bash
git add lib/supabase/middleware.ts lib/supabase/middleware.test.ts middleware.ts vitest.config.ts package.json package-lock.json
git commit -m "feat: add auth middleware protecting routes"
```

---

### Task 5: Página de login

**Files:**
- Create: `app/login/page.tsx`
- Create: `app/login/actions.ts`

**Interfaces:**
- Consumes: `createClient()` de `lib/supabase/server.ts`.
- Produces: rota `/login` com formulário email/senha que chama a Server Action `login`.

- [ ] **Step 1: Criar a Server Action de login**

```ts
// app/login/actions.ts
"use server";

import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

export async function login(formData: FormData) {
  const email = String(formData.get("email"));
  const password = String(formData.get("password"));

  const supabase = await createClient();
  const { error } = await supabase.auth.signInWithPassword({ email, password });

  if (error) {
    redirect(`/login?error=${encodeURIComponent(error.message)}`);
  }

  redirect("/dashboard");
}
```

- [ ] **Step 2: Criar a página de login**

```tsx
// app/login/page.tsx
import { login } from "./actions";

export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string }>;
}) {
  const { error } = await searchParams;

  return (
    <main className="flex min-h-screen items-center justify-center">
      <form action={login} className="flex w-72 flex-col gap-3">
        <h1 className="text-lg font-semibold">Entrar</h1>
        {error && <p className="text-sm text-red-600">{error}</p>}
        <input
          name="email"
          type="email"
          placeholder="email"
          required
          className="rounded border px-3 py-2"
        />
        <input
          name="password"
          type="password"
          placeholder="senha"
          required
          className="rounded border px-3 py-2"
        />
        <button type="submit" className="rounded bg-black py-2 text-white">
          Entrar
        </button>
      </form>
    </main>
  );
}
```

- [ ] **Step 3: Verificar build**

Run: `npm run build`
Expected: build conclui sem erros.

- [ ] **Step 4: Teste manual de login**

Run: `npm run dev`, abra `http://localhost:3000/login`, entre com o email/senha criados no pré-requisito manual (Supabase → Authentication → Users).
Expected: login bem-sucedido redireciona para `/dashboard` (ainda 404 até a Task 6 — tudo bem, confirma que o redirect ocorreu).

- [ ] **Step 5: Commit**

```bash
git add app/login
git commit -m "feat: add login page"
```

---

### Task 6: Dashboard placeholder protegido + logout

**Files:**
- Create: `app/dashboard/page.tsx`
- Create: `app/dashboard/logout-button.tsx`
- Modify: `app/page.tsx`

**Interfaces:**
- Consumes: `createClient()` de `lib/supabase/server.ts` e `lib/supabase/client.ts`.
- Produces: rota `/dashboard` protegida (via middleware da Task 4) mostrando o email do usuário logado e um botão de logout.

- [ ] **Step 1: Criar o botão de logout (Client Component)**

```tsx
// app/dashboard/logout-button.tsx
"use client";

import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";

export function LogoutButton() {
  const router = useRouter();
  const supabase = createClient();

  return (
    <button
      onClick={async () => {
        await supabase.auth.signOut();
        router.push("/login");
      }}
      className="rounded border px-3 py-1 text-sm"
    >
      Sair
    </button>
  );
}
```

- [ ] **Step 2: Criar a página do dashboard**

```tsx
// app/dashboard/page.tsx
import { createClient } from "@/lib/supabase/server";
import { LogoutButton } from "./logout-button";

export default async function DashboardPage() {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4">
      <p>Logado como {user?.email}</p>
      <LogoutButton />
    </main>
  );
}
```

- [ ] **Step 3: Fazer `/` redirecionar para `/dashboard`**

```tsx
// app/page.tsx
import { redirect } from "next/navigation";

export default function Home() {
  redirect("/dashboard");
}
```

- [ ] **Step 4: Verificar build**

Run: `npm run build`
Expected: build conclui sem erros.

- [ ] **Step 5: Teste manual ponta a ponta**

Run: `npm run dev`. Em uma aba anônima, acesse `http://localhost:3000/`.
Expected: redireciona para `/login` (sem sessão). Faça login → redireciona para `/dashboard` mostrando seu email. Clique "Sair" → volta para `/login`. Tentar acessar `/dashboard` direto sem sessão deve redirecionar de volta para `/login`.

- [ ] **Step 6: Commit**

```bash
git add app/dashboard app/page.tsx
git commit -m "feat: add protected dashboard placeholder with logout"
```

---

### Task 7: Deploy inicial na Vercel

**Files:**
- Create: `README.md` (instruções de setup e deploy)

**Interfaces:**
- Produces: site publicado acessível por URL da Vercel, com login funcionando em produção.

- [ ] **Step 1: Documentar variáveis de ambiente no README**

```markdown
# Controle de Gastos

## Setup local
1. `npm install`
2. Copie `.env.local.example` para `.env.local` e preencha com as credenciais do seu projeto Supabase.
3. Rode a migração `supabase/migrations/0001_init.sql` no SQL Editor do Supabase.
4. Crie seu usuário em Supabase → Authentication → Users.
5. `npm run dev`

## Deploy (Vercel)
1. Importe o repositório na Vercel.
2. Em Project Settings → Environment Variables, adicione `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY` com os mesmos valores do `.env.local`.
3. Deploy.
```

- [ ] **Step 2: Push do repositório para o GitHub (se ainda não tiver remoto)**

Peça ao usuário para criar um repositório vazio no GitHub e confirme antes de:

```bash
git remote add origin <url-do-repo>
git push -u origin master
```

- [ ] **Step 3: Importar o projeto na Vercel**

No painel da Vercel, "Add New Project" → selecione o repositório → configure as duas env vars do Step 1 → Deploy.
Expected: build passa, URL de produção acessível.

- [ ] **Step 4: Teste manual em produção**

Acesse a URL de produção, faça login com as credenciais reais.
Expected: mesmo comportamento do teste manual da Task 6, agora em produção.

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: add setup and deploy instructions"
```

---

## Próximas fases (não incluídas neste plano)

- **Fase 2:** Upload de PDF + parser Nubank + tela de revisão manual (fluxo ponta a ponta com um banco).
- **Fase 3:** Parsers Sicoob Credivar, Banco do Brasil, Mercado Pago + motor de categorização por regras.
- **Fase 4:** Orçamento mensal por categoria, comparação histórica e dashboard de alertas.

Cada uma será escrita como um plano próprio quando a fase anterior estiver concluída.
